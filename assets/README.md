# Image assets — drop your content images here

The microsite works fine without these (it shows clean gradient placeholders).
To fill in real Luma imagery, add files with the **exact filenames** below into this
`assets/` folder. No code changes needed — the paths are already wired up.

| Filename              | Where it appears                                   | Suggested size (px) | Notes |
|-----------------------|----------------------------------------------------|---------------------|-------|
| `persona-marcus.jpg`  | Persona avatar (Marcus Webb), left panel + chips   | 200 × 200 (square)  | Head/shoulders, centered. Falls back to "MW" initials. |
| `persona-anna.jpg`    | Persona avatar (Anna Kowalska)                     | 200 × 200 (square)  | Falls back to "AK" initials. |
| `persona-lukas.jpg`   | Persona avatar (Lukas Bergmann)                    | 200 × 200 (square)  | Falls back to "LB" initials. |
| `luma-mobile.jpg`           | Mobile card — **Joined** moment (also non-member)  | 640 × 360 (16:9)    | Invitation / hero shot. |
| `luma-hero.jpg`             | Website card — **Joined** moment                   | 1200 × 600 (2:1)    | Wide invitation / hero shot. |
| `luma-mobile-progress.jpg`  | Mobile card — **Completed a task** (in progress)   | 640 × 360 (16:9)    | Active / mid-workout shot. |
| `luma-hero-progress.jpg`    | Website card — **Completed a task**                | 1200 × 600 (2:1)    | Wide in-progress shot. |
| `luma-mobile-complete.jpg`  | Mobile card — **Hit the final milestone**          | 640 × 360 (16:9)    | Celebration / achievement shot. |
| `luma-hero-complete.jpg`    | Website card — **Hit the final milestone**         | 1200 × 600 (2:1)    | Wide celebration shot. |
| `luma-mobile-upgraded.jpg`  | Mobile card — **Tier upgraded**                    | 640 × 360 (16:9)    | Premium / elevated shot. |
| `luma-hero-upgraded.jpg`    | Website card — **Tier upgraded**                   | 1200 × 600 (2:1)    | Wide premium shot. |

The content-card image now changes with the journey moment (the bar at the top of the page).

## Tips
- Keep files reasonably small (< 400 KB each) so the page stays fast.
- JPG or PNG both work. Use the exact filenames above (case-sensitive).
- If a file is missing, the design degrades gracefully to a gradient + initials —
  nothing breaks.
